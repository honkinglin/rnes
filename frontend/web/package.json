{
  "name": "rnes-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build-wasm": "cd ../../ && wasm-pack build frontend/web --target web --out-dir frontend/web/pkg",
    "dev-full": "npm run build-wasm && npm run dev"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
